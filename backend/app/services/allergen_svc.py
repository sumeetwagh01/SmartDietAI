import os

MODEL_PATH = os.path.join("models", "allergen_classifier.h5")

def detect_allergens(food_name: str):
    # TODO: load TF model and predict allergen classes
    # from tensorflow.keras.models import load_model
    return []   # e.g. ["peanuts", "gluten"]
